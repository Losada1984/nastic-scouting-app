{
  "scripts": {
    "dev": "pip install -r requirements.txt && streamlit run main.py --server.port 3000 --server.address 0.0.0.0",
    "start": "streamlit run main.py --server.port 3000 --server.address 0.0.0.0"
  }
}
